<Window x:Class="LoginWindow.Views.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginWindow.Views"
        mc:Ignorable="d"
        Title="CustomMessageBox" Width="420" Height="250"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Background="#1E1E1D"
        >
    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:StarfieldBackground />
        <!-- 消息内容（支持换行） -->
        <TextBlock Grid.Row="0" Text="{Binding Message}" TextWrapping="Wrap" Foreground="#FFFFFF" x:Name="WindowMessage"
                   FontSize="28" LineHeight="22" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <!-- 按钮区 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button x:Name="BtnCancel" Content="取 消" MinWidth="80" Margin="0,0,12,0"
                    Style="{StaticResource ButtonDefault}" Click="OnCancel"/>
            <Button x:Name="BtnConfirm" Content="确 定" MinWidth="80"
                    Style="{StaticResource ButtonPrimary}" Click="OnConfirm"/>
        </StackPanel>
    </Grid>
</Window>
