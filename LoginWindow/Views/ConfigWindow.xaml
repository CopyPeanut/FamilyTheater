<Window x:Class="LoginWindow.Views.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginWindow.Views"
        mc:Ignorable="d"
        Background="#1E1E1D"
        Style="{DynamicResource WindowBaseStyle}"
        Title="" Height="300" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" CornerRadius="0,0,10,10" Background="#f1b054" Padding="15,8">
            <TextBlock Text="设置" FontSize="20" FontWeight="Bold" Foreground="#1E1E1D"/>
        </Border>

        <!-- 内容区 -->
        <Grid Grid.Row="1" Margin="30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 媒体根目录 -->
            <TextBlock Grid.Row="0" Text="媒体根目录" Foreground="#f1b054" FontSize="14" Margin="0,0,0,8"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Height="35"
                         Text="{Binding MediaRootPath, UpdateSourceTrigger=PropertyChanged}"
                         Background="#333333" Foreground="White" BorderBrush="#555555"
                         BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" FontSize="14">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#f1b054"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border" CornerRadius="8" Background="#333333" BorderBrush="#555555" BorderThickness="1"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                              HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter Property="BorderBrush" TargetName="border" Value="#f1b054"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="true">
                                    <Setter Property="BorderBrush" TargetName="border" Value="#f1b054"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
                <Button Grid.Column="1" Content="浏览..." Command="{Binding BrowseCommand}"
                        Height="35" Margin="10,0,0,0" Padding="15,0"
                        Background="#f1b054" Foreground="#1E1E1D" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                        FontSize="14"/>
            </Grid>

            <!-- 状态消息 -->
            <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="Gray" FontSize="12" Margin="0,10,0,0"/>
        </Grid>

        <!-- 底部保存按钮 -->
        <Border Grid.Row="2" Background="#f1b054" CornerRadius="10,10,0,0" Padding="15,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="保存" Command="{Binding SaveCommand}"
                        Width="100" Height="32"
                        Background="#1E1E1D" Foreground="#f1b054" FontWeight="Bold"
                        BorderThickness="0" Cursor="Hand" FontSize="14"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>