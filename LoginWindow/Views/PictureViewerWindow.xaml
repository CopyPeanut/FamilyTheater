<Window x:Class="LoginWindow.Views.PictureViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="图片预览" Background="#1E1E1D" WindowState="Maximized"
        WindowStyle="SingleBorderWindow" ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 图片区域 -->
        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Image x:Name="ImageViewer" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </ScrollViewer>

        <!-- 控制栏 -->
        <Grid Grid.Row="1" Background="#1E1E1D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 文件名显示 -->
            <TextBlock x:Name="FileNameDisplay" Grid.Column="0" VerticalAlignment="Center"
                       Foreground="#CCCCCC" FontSize="13" Margin="15,0" TextTrimming="CharacterEllipsis"/>

            <!-- 关闭按钮 -->
            <Button Grid.Column="1" Click="CloseBtn_Click"
                    Width="40" Height="32" Margin="8,4" Background="Transparent"
                    BorderThickness="0" Foreground="#f1b054" FontSize="16"
                    Cursor="Hand" Content="✕"/>
        </Grid>
    </Grid>
</Window>
