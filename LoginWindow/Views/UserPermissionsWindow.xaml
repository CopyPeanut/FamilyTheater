<Window x:Class="LoginWindow.Views.UserPermissionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginWindow.Views"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Background="#1E1E1D"
        Style="{DynamicResource WindowBaseStyle}"
        Title="用户权限管理" Height="620" Width="760">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <local:StarfieldBackground StarCount="70" />

        <Border Margin="28"
                BorderBrush="#f1b054"
                BorderThickness="1"
                CornerRadius="10"
                Background="#CC1E1E1D"
                Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="用户权限管理"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,0,0,8"/>

                <Grid Grid.Row="1" Margin="0,10,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="用户"
                               Foreground="#f1b054"
                               FontWeight="Bold"
                               FontSize="15"/>
                    <TextBlock Grid.Column="1"
                               Text="权限"
                               Foreground="#f1b054"
                               FontWeight="Bold"
                               FontSize="15"/>
                    <TextBlock Grid.Column="2"
                               Text="操作"
                               Foreground="#f1b054"
                               FontWeight="Bold"
                               FontSize="15"
                               HorizontalAlignment="Right"/>
                </Grid>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Users}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#3A3A38"
                                        BorderThickness="0,1,0,0"
                                        Padding="0,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="180"/>
                                            <ColumnDefinition Width="120"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Username}"
                                                       Foreground="White"
                                                       FontSize="16"/>
                                            <TextBlock Text="admin 账户不能在这里降级"
                                                       Foreground="#888888"
                                                       FontSize="12"
                                                       Margin="0,4,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsEditable}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>

                                        <ComboBox Grid.Column="1"
                                                  Width="150"
                                                  Height="34"
                                                  ItemsSource="{Binding RoleOptions}"
                                                  SelectedItem="{Binding SelectedRole, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  IsEnabled="{Binding IsEditable}"
                                                  VerticalAlignment="Center"/>

                                        <Button Grid.Column="2"
                                                Content="保存"
                                                Command="{Binding DataContext.SaveRoleCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                IsEnabled="{Binding IsEditable}"
                                                Width="86"
                                                Height="34"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Background="#f1b054"
                                                Foreground="Black"
                                                BorderThickness="0"
                                                Cursor="Hand"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock Grid.Row="3"
                           Text="{Binding StatusMessage}"
                           Foreground="#f1b054"
                           FontSize="13"
                           TextWrapping="Wrap"
                           Margin="0,16,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
